
Confirm OPROFILE are unselected (either y or m) in kernel configuration
CONFIG_OPROFILE=y
CONFIG_OPROFILE=m

Confirm PROFILING & PERF_EVENTS are selected in kernel configuration 
CONFIG_PERF_EVENTS=y
CONFIG_HW_PERF_EVENTS=y
CONFIG_PROFILING=y

example:

operf --events INSTRUCTIONS:500 ./wakeup

opreport -l wakeup

